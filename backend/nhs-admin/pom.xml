<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>nhs-server</artifactId>
        <groupId>group.aitools.nhs</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <artifactId>nhs-admin</artifactId>

    <description>
        web服务入口
    </description>

    <dependencies>

        <!-- PostgreSQL driver: the private deployment uses PostgreSQL as its primary database. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

<!--        &lt;!&ndash; mp支持的数据库均支持 只需要增加对应的jdbc依赖即可 &ndash;&gt;-->
<!--        &lt;!&ndash; Oracle &ndash;&gt;-->
<!--        <dependency>-->
<!--            <groupId>com.oracle.database.jdbc</groupId>-->
<!--            <artifactId>ojdbc8</artifactId>-->
<!--        </dependency>-->
<!--        &lt;!&ndash; 兼容oracle低版本 &ndash;&gt;-->
<!--        <dependency>-->
<!--            <groupId>com.oracle.database.nls</groupId>-->
<!--            <artifactId>orai18n</artifactId>-->
<!--        </dependency>-->
<!--        &lt;!&ndash; PostgreSql &ndash;&gt;-->
<!--        <dependency>-->
<!--            <groupId>org.postgresql</groupId>-->
<!--            <artifactId>postgresql</artifactId>-->
<!--        </dependency>-->
<!--        &lt;!&ndash; SqlServer &ndash;&gt;-->
<!--        <dependency>-->
<!--            <groupId>com.microsoft.sqlserver</groupId>-->
<!--            <artifactId>mssql-jdbc</artifactId>-->
<!--        </dependency>-->

        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-common-doc</artifactId>
        </dependency>

        <!-- 邮件服务 -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-common-mail</artifactId>
        </dependency>

        <!-- mcp模块 -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-common-mcp</artifactId>
        </dependency>

        <!-- api模块 -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-api</artifactId>
        </dependency>

        <!-- 系统模块 -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-system</artifactId>
        </dependency>

        <!-- 调度任务模块 -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-job</artifactId>
        </dependency>

        <!-- AI业务模块 -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-ai</artifactId>
        </dependency>

        <!-- Agent platform business module -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-platform</artifactId>
        </dependency>

        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-runtime-agentscope</artifactId>
        </dependency>

        <!--  工作流模块  -->
        <dependency>
            <groupId>group.aitools.nhs</groupId>
            <artifactId>nhs-workflow</artifactId>
        </dependency>

        <!-- Spring Boot Admin 客户端 -->
        <dependency>
            <groupId>de.codecentric</groupId>
            <artifactId>spring-boot-admin-starter-client</artifactId>
        </dependency>

        <!-- 单元测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- skywalking 整合 logback -->
<!--        <dependency>-->
<!--            <groupId>org.apache.skywalking</groupId>-->
<!--            <artifactId>apm-toolkit-logback-1.x</artifactId>-->
<!--            <version>${与你的agent探针版本保持一致}</version>-->
<!--        </dependency>-->
<!--        <dependency>-->
<!--            <groupId>org.apache.skywalking</groupId>-->
<!--            <artifactId>apm-toolkit-trace</artifactId>-->
<!--            <version>${与你的agent探针版本保持一致}</version>-->
<!--        </dependency>-->

    </dependencies>


    <profiles>
        <!-- 仅在激活 'gen' profile 时，才引入代码生成模块 -->
        <profile>
            <id>gen</id>
            <dependencies>
                <!-- 代码生成-->
                <dependency>
                    <groupId>group.aitools.nhs</groupId>
                    <artifactId>nhs-gen</artifactId>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven-jar-plugin.version}</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>${maven-war-plugin.version}</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <warName>${project.artifactId}</warName>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
