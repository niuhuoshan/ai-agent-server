package group.aitools.nhs.platform.iam.domain;

/**
 * 定义任务Visibility相关的可选值。
 * Visibility modes supported by first-phase tasks. */
public enum TaskVisibility {
    ENTERPRISE_SHARED,
    RESTRICTED
}
